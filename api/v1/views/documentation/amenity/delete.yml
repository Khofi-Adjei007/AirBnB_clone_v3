# This operation removes an amenity by its ID
---
tags:
  - Amenities
parameters:
  # 'place_id' is the unique ID of the place to be deleted
  - name: place_id
    in: path
    type: string
    required: true
    description: the unique ID of the place to be deleted
  # 'amenity_id' is the ID of the amenity to be removed
  - name: amenity_id
    in: path
    type: string
    required: true
    description: amenity ID to remove
responses:
  404:
    # Responds with a 404 status code for various situations
    description: place not found / amenity not found / amenity not found in place 😳
  200:
    # Responds with a 200 status code for a successful request
    description: Successful request ✔️
