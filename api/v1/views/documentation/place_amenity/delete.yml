# Removes an amenity by ID.
---
tags:
  - Place_Amenities
parameters:
  - name: place_id
    in: path
    type: string
    required: true
    # The unique ID of the place with the amenity to be deleted.
    description: the unique id of the place with the amenity to be delete
  - name: amenity_id
    in: path
    type: string
    required: true
    # The ID of the amenity to remove.
    description: amenity id to remove
responses:
  404:
    # Responds with a 404 status code if the place or amenity is not found,
    description: place not found / amenity not found / amenity not found in place 😳
  200:
    # Responds with a 200 status code for a successful request.
    description: Successful request ✔️
